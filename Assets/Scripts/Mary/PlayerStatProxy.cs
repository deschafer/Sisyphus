using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerStatProxy : MonoBehaviour
{
    public int health = 0;
    public int speed;
    public int attack;
    public int jump; 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
